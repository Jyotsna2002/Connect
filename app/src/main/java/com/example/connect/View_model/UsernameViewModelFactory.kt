package com.example.connect.View_model

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.example.connect.Repository.HomePageRepo
import com.example.connect.Repository.UploadPostRepo
import com.example.connect.Repository.UsernameRepo

//class UsernameViewModelFactory(val usernameRepo: UsernameRepo) : ViewModelProvider.Factory {
////    override fun <T : ViewModel> create(modelClass: Class<T>): T {
////        return modelClass.getConstructor(UsernameRepo::class.java).newInstance(usernameRepo)
////    }
//}